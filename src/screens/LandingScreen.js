import React from "react";
import { Text, View, StyleSheet } from "react-native";

/*
  -- DOCUMENTATION --
*/
const LandingScreen = (props) => {
    return (
        <View>
            <Text>Hello, you're in landing!</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default LandingScreen;
